# CLAUDE.md

This file provides guidance to Claude Code (claude.ai/code) when working with code in this repository.

## 工作流编排

### 1. 默认规划节点 (Plan Node Default)
- 对于任何非平凡任务（3个以上步骤或涉及架构决策），**必须**进入规划模式。
- 如果事情偏离轨道，**立即停止**并重新规划——不要强行推进。
- 将规划模式用于验证步骤，而不仅仅是构建步骤。
- 预先编写详细的规格说明以减少歧义。

### 2. 子代理策略 (Subagent Strategy)
- 大量使用子代理以保持主上下文窗口整洁。
- 将研究、探索和并行分析工作卸载给子代理。
- 对于复杂问题，通过子代理投入更多算力。
- 每个子代理专注于一个具体的切入点以确保执行聚焦。

### 3. 自我改进循环 (Self-Improvement Loop)
- 收到用户纠正后，先判断它是否暴露了跨任务、跨模块可复用的原则或方法论。
- 只有具备普适性的模式才进入 `tasks/lessons.md`；一次性事实、具体路径、端口、临时实现细节不生成全局规则。
- 新增规则前先查是否能合并到已有条目，优先改写和压缩旧规则，避免按纠正次数堆叠条目。
- 在会话开始时按当前任务定向审查相关教训，不为了“了解背景”全文扩散。

### 4. 完成前验证 (Verification Before Done)
- 在没有证明其有效之前，**绝不**标记任务为完成。
- 在相关情况下，对比主分支与你更改后的行为差异。
- 自问：“资深工程师会批准这个吗？”
- 运行测试、检查日志、展示正确性。

### 5. 追求优雅（平衡之道）(Demand Elegance - Balanced)
- 对于非平凡的变更：暂停并自问“是否有更优雅的方式？”
- 如果修复感觉像是在打补丁（hacky）： “鉴于我现在所知的一切，实施那个优雅的解决方案。”
- 对于简单、明显的修复可跳过此步——不要过度工程化。
- 在提交成果前先挑战自己的工作。

### 6. 自主缺陷修复 (Autonomous Bug Fixing)
- 当收到缺陷报告时：**直接修复它**。不要请求手把手的指导。
- 指向日志、错误或失败的测试——然后解决它们。
- 无需用户进行任何上下文切换。
- 主动修复失败的 CI 测试，无需被告知如何做。

## 任务管理（Scrum 框架）

### 文档结构
```
tasks/
├── product_backlog.md          # 产品待办列表（Epic + Story）
├── sprints/
│   └── sprintN/
│       ├── planning.md         # 迭代规划
│       ├── design.md           # 迭代设计
│       ├── review.md           # 迭代评审
│       └── retrospective.md    # 迭代回顾
└── lessons.md                  # 经验教训汇总
```

### Sprint Planning（迭代规划）
1. **选取 Story**：从 `product_backlog.md` 选取优先级高的 Story
2. **任务分解**：将 Story 分解为具体的 Task，写入 `sprintN/planning.md`
3. **估算工作量**：评估每个 Story 的工作量（天数）
4. **明确验收标准**：每个 Story 必须有清晰的验收标准
5. **识别风险**：列出技术风险和缓解措施
6. **记录设计决策**：重要的架构决策要记录理由和影响

### Sprint 执行
1. **追踪进度**：在 `planning.md` 中标记任务完成状态（[x]）
2. **同步更新**：每完成一个 Story，立即更新文档
3. **代码审查**：每个 Story 完成后立即进行代码审查
4. **测试验证**：运行测试、检查日志、展示正确性
5. **文档同步**：代码和文档必须同步更新

### Sprint Review（迭代评审）
1. **验收 Story**：逐个验收 Story，确认是否达成验收标准
2. **功能演示**：演示已完成的功能
3. **识别技术债务**：记录发现的技术债务和改进计划
4. **更新产品待办**：将已完成的 Story 标记为 ✅，新增 Story 标记为 📋
5. **规划下一个 Sprint**：确定下一个 Sprint 的目标

### Sprint Retrospective（迭代回顾）
1. **做得好的地方（Keep）**：总结成功经验，继续保持
2. **需要改进的地方（Problem）**：识别问题、分析根因、制定改进措施
3. **尝试的新做法（Try）**：评估新做法的效果，决定是否继续
4. **行动项（Action Items）**：明确负责人、截止日期、状态跟踪
5. **经验教训固化**：将关键教训提炼到 `lessons.md` 和 `CLAUDE.md`

### Story 管理
- **Epic**：大的功能模块（如"数据库闭环能力"）
- **Story**：可独立交付的功能点（如"SQLite 驱动实现"）
- **Task**：Story 的具体实施步骤（如"实现 SqliteBatchReader"）
- **状态标识**：✅ 已完成、🚧 进行中、📋 待规划、💡 设计阶段

### 经验教训管理
1. **及时记录**：收到用户纠正后，立即更新 `tasks/lessons.md`
2. **分类整理**：按主题分类（架构、测试、代码质量等）
3. **固化规则**：将关键教训提炼为规则，补充到 `CLAUDE.md`
4. **定期回顾**：会话开始时审查相关教训，避免重复错误

### 文档更新原则
1. **代码与文档同步**：每个 Story 完成后立即更新文档
2. **文档要准确**：确保文档与实际代码一致
3. **文档要完整**：用户手册、开发者指南、API 文档都要完善

## Agent 架构：Plan-and-Solve + Reflexion

四层流水线，详细设计见 `tasks/sprints/sprint-refactor/planning.md`。

```
规划层(Plan) → 工具执行(Tool) → 执行层(Solve) → 反思层(Reflexion)
```

核心约束：
- **不是 ReAct**：规划层一次性输出 `tool_calls`，编排层按序执行，不做 LLM 循环决策
- **先缓冲再流式**：执行层完整输出 → 反思层检查 → 再流式发送
- **反思两级检查**：正则必过（每轮零开销），LLM 按需触发（仅 `difficulty=="困难"` 或用户不满）
- **图片走摘要**：图片以摘要文本传入规划层，base64 只在工具执行时使用
- **学生优先**：学生要求直接答案时，strategy 必须切为 `direct_answer`
- **会话状态由编排层管**：`current_question`/`current_knowledge` 在 `ChatSession` 表，工具调用后更新；`context_ready` 由 `current_question IS NOT NULL` 推导
- **模型配置**：默认供应商共享 `llm_api_url`/`llm_api_key`，6 个场景各自配模型名，按需覆盖 `_url`/`_key`

## 核心原则

- **简洁优先**：使每次变更尽可能简单。影响最小化的代码。
- **拒绝懒惰**：寻找根本原因。杜绝临时修复。遵循资深开发者标准。
- **最小影响**：变更应仅触及必要部分。避免引入新的 bug。

## 交流规则

1. 全程使用中文交流
2. 需要编译时直接编译，无需确认
3. 移动或删除目录前需要用户确认
4. 不要自动提交代码（git commit/push），等用户明确发起提交指令后再执行

## 编程规则

- 尽量使用成熟的开源方案，不重复造轮子
- 给代码提供必要的中文注释
- 新建或修改 `*.h/*.hpp/*.cpp/*.cc/*.py/*.go` 文件时，文件头必须使用统一版权声明：
  `Copyright (C) 2026 LIHUO` + MIT License 说明块
- 问题处理必须先核查再修改（强制）：
  1. 收到问题后先做事实核查（日志/代码路径/接口返回/测试复现），禁止直接改代码
  2. 核查确认是问题：先给出最小修改方案（影响面、风险、验证方式），再实施修改
  3. 核查确认不是问题：必须提供可复核证据（命令输出、代码位置、接口响应）并说明不改原因
  4. 任何“未核查先修改”的行为都视为流程违规
