


这是一份为你量身定制的**《Study Helper 智能学习助手 开发实施计划书》**。作为软件工程师，你可以直接将这份文档作为项目的 README 或者导入到你的项目管理工具（如 Jira、Notion、Teambition）中作为 Epics 和 Tasks。

---

# Study Helper 智能学习助手 - 开发实施计划书 (MVP 阶段)

## 1. 项目概述
*   **产品定位：** 专注于辅助中小学生学习的 AI 智能体应用，提供苏格拉底式的答疑、个性化薄弱点强化及日常练习。
*   **产品形态：** 跨端应用（前期 H5 Web 端跑通全链路，中期上线微信小程序，后期打包 App）。
*   **核心策略：** “重 AI 编排，轻本地数据”。前期不自建庞大题库，利用大模型泛化能力和向量库检索历史数据。

## 2. 技术栈选型
| 模块 | 技术选型 | 备注说明 |
| :--- | :--- | :--- |
| **前端架构** | Uni-app (Vue 3) / Taro (React) | 跨端框架，一套代码编译 H5 和微信小程序 |
| **公式与渲染** | `markdown-it` + `KaTeX` / `MathJax` | 核心难点：解析大模型返回的 LaTeX 数学公式 |
| **AI 编排层** | Dify (Docker 开源自部署) | 将 AI Prompt、打标、工作流可视化编排并暴露 API |
| **业务后端** | Python (FastAPI) | 承担用户鉴权、数据持久化、请求中转分发 |
| **数据库** | PostgreSQL + `pgvector` 扩展 | 关系型数据存用户/记录，向量存储用于相似题检索 |
| **底层大模型** | Qwen-VL-Max / Claude 3.5 / GPT-4o | 视觉多模态能力强，作为 OCR 和核心解题大脑 |

---

## 3. 核心业务数据流转设计

### 3.1 核心答疑链路 (拍照 -> 解析 -> 反馈)
1.  **用户**通过前端拍照/传图上传。
2.  **FastAPI** 接收图片，完成校验/压缩，通过 **Dify** 文件上传接口获取 `file_id`，同时将图片保存到对象存储或本地静态目录用于预览、历史追溯和审计。
3.  **Dify Workflow** 执行：
    *   *节点 A (多模态识别)：* 识别学科、提取题干文本。
    *   *节点 B (知识点提取)：* 输出该题目的二级知识点标签（如：初中数学-勾股定理）。
    *   *节点 C (苏格拉底解答)：* 严格限制**不直接给最终答案**，先给出解题思路和第一步。
4.  **前端**通过 SSE（Server-Sent Events）接收流式输出，实时渲染 Markdown 和 LaTeX 公式。
5.  **用户**点击反馈按钮：
    *   **`<我不甚理解>`**：发送特定指令，Dify 进入“分步拆解”分支，输出更细致的下一步。
    *   **`<我会了>`**：Dify 进入“总结”分支，并触发生成一道“相似题”的指令。

### 3.2 几何图形渲染方案
*   **AI 侧：** Prompt 约束大模型在遇到几何题时，按需输出 `python:figure` 代码块，使用 `matplotlib` 描述辅助线、角度标注和关键几何关系。
*   **后端侧：** FastAPI 调用独立 Docker 沙箱执行绘图代码，生成图片并存储到对象存储或静态文件服务，返回图片 URL。
*   **前端侧：** 识别 `python:figure` 代码块，提交后端沙箱执行，并将返回的图片 URL 渲染为几何图。

---

## 4. 实施路径 (Action Plan)

计划分为 4 个 Sprint (迭代周期)，预计耗时 4-6 周。

### Sprint 1: AI 引擎与基础设施搭建（已闭环）
*   [x] **任务 1.1**：本地 Docker 环境可用。
*   [x] **任务 1.2**：部署开源版 Dify。
*   [x] **任务 1.3**：在 Dify 中配置模型凭证。
*   [x] **任务 1.4（核心）**：在 Dify 中建立 Chatflow，跑通图片识别、知识点提取、多轮引导、直接解答和新题切换。
*   [x] **边界确认**：Python 绘图执行不放在 Dify 内部，转入后端沙箱和前端展示链路。

### Sprint 2: 业务后端与数据库搭建（已闭环）
*   [x] **Story 2.1**：初始化 FastAPI 工程，完成项目结构、路由、配置管理、日志、异常处理和健康检查。
*   [x] **Story 2.2**：设计并创建 PostgreSQL 数据库表：`users`（匿名占位）、`chat_sessions`、`chat_messages`、`user_tags_history`、`assets`，并通过 Alembic 管理迁移。
*   [x] **Story 2.3**：对接 Dify API，封装流式聊天接口 `/api/v1/chat/completions`，返回 SSE，并映射 Dify `conversation_id/message_id`。
*   [x] **Story 2.4**：实现图片上传代理，调用 Dify `/v1/files/upload` 获取 `file_id`，本地或 MinIO 存储仅用于预览、审计和历史追溯。
*   [x] **Story 2.5**：实现对话管理：创建/续接会话、获取历史消息、会话列表、同步新题目/继续追问/直接解答状态。
*   [x] **Story 2.6**：实现 Python 绘图代码沙箱，隔离执行 `python:figure`，生成图片并返回 `asset_id` 或 URL。
*   [x] **Story 4.5**：建立最小核心答疑回归集，覆盖真实题图、Dify 代理、上传、会话和沙箱关键路径。

### Sprint Refactor: 后端 Agent 架构重构（已完成）
*   [x] **R.1**：搭建 Agent 基础设施（openai SDK、LLM 客户端、6 场景模型配置）。
*   [x] **R.2**：实现 Agent 工具层（process_question + extract_knowledge）。
*   [x] **R.3**：实现规划层（plan）和执行层（solve）。
*   [x] **R.4**：实现反思层（正则 Level 1 + LLM Level 2）和 Agent 编排服务。
*   [x] **R.5**：重写 ChatService 和文件上传（移除 Dify 依赖）。
*   [x] **R.6**：清理 Dify 代码依赖（删除 dify.py、相关字段、Alembic 迁移）。
*   [x] **R.7**：更新 Docker 配置（移除 Dify 环境变量，添加 LLM API 配置）。
*   [x] **R.8**：更新文档（framework.md、planning.md）。

### Sprint 3: 跨端前端搭建与核心答疑闭环（进行中，约 10.5 天）
*   [ ] **Story 3.1**：初始化 Uni-app (Vue 3) 项目，搭建基础 UI 框架（首页、聊天页、历史页 TabBar）。
*   [ ] **Story 3.2**：实现聊天对话页：消息气泡列表 + 输入框 + 发送按钮。
*   [ ] **Story 3.3 (难点)**：实现流式输出打字机效果：对接后端 SSE 接口，逐字渲染 AI 回复。
*   [ ] **Story 3.4 (难点)**：集成 markdown-it + KaTeX：渲染 AI 回复中的 Markdown 格式和数学公式。
*   [ ] **Story 3.5**：实现拍照/选图上传：调用摄像头或相册，压缩图片后上传到后端。
*   [ ] **Story 3.6**：图片预览：上传后在聊天框中展示图片缩略图。
*   [ ] **Story 3.7 + 4.4**：实现 AI 回复代码块识别与 python:figure 执行渲染（合并实施）。
*   [ ] **Story 4.1**：Dify 工作流完善「分步拆解」分支。
*   [ ] **Story 4.2**：Dify 工作流完善「总结 + 相似题」分支。
*   [ ] **Story 4.3**：前端实现「不甚理解」/「我会了」悬浮反馈按钮。

### Sprint 4: 业务闭环、演练与多端打包 (1.5周)
*   [ ] **任务 4.1**：完成”个人数据库（历史记录/错题本）”的前后端联调，展示用户各学科的薄弱知识点雷达图。
*   [ ] **任务 4.2**：实现”日常练习”模块（H5 页面上选择年级，调用大模型生成当天的 5 个单词或 1 句古诗）。
*   [ ] **任务 4.3**：H5 兼容性测试，并在微信开发者工具中打包预览微信小程序，处理微信特定的网络白名单和权限校验。

---

## 5. 前期开发核心避坑指南 (Tips)

1.  **公式渲染的坑：** 大模型吐出的公式格式经常不统一（有时候用 `$...$`，有时候用 `\[...\]`，有时候转义符会丢失如 `\\n`）。**对策：** 必须在 Dify 的 Prompt 中极其严格地要求它使用标准且唯一的数学公式边界符（推荐块级公式用 `$$...$$`，行内用 `\(...\)`），并在前端做正则预处理清洗。
2.  **大模型算力幻觉：** 纯文本大模型做初高中数学计算题很容易在最后一步算错。**对策：** 尽量选用带有强大推理能力或内置 Code Interpreter 的模型（如 Qwen-Max, DeepSeek-Coder），并在 Prompt 中加上 *“请一步一步思考，并在给出最终答案前复核你的计算过程”* (CoT 思维链)。
3.  **响应耗时：** 多模态图片解析通常较慢（可能长达 3-8 秒首字返回）。**对策：** 前端必须做足 Loading 动画，上传图片后立即在聊天框展示出图片预览和进度条，缓解用户等待焦虑。

---

**下一步建议：**
Sprint 1、Sprint 2 已完成。当前进入 **Sprint 3**，按 `tasks/sprints/sprint3/planning.md` 实施跨端前端搭建（Uni-app + uv-ui）、SSE 打字机效果、Markdown/KaTeX 渲染、图片上传预览、代码块识别与沙箱执行、Dify 反馈分支和前端反馈按钮。
